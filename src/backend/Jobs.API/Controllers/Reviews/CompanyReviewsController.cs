using Jobs.Domain.Entities;
using Microsoft.Extensions.Hosting;
using System;
using System.ComponentModel.Design;

namespace Jobs.API.Controllers.Reviews
{
    public class CompanyReviewsController
    {
    }
}

//| Method | Route                                      | Description |
//| ------ | ----------------------------------         | ------------------------- |
//| GET    | `/ api / reviews / company /{ companyId}`  | Get reviews for a company |
//| POST   | `/api/reviews`                             | Add review                |
//| DELETE | `/api/reviews/{id}`                        | Delete review |

////GET / api / companies /{ companyId}/ reviews
////POST / api / companies /{ companyId}/ reviews
////PUT / api / companies /{ companyId}/ reviews /{ id}
////DELETE / api / companies /{ companyId}/ reviews /{ id}

